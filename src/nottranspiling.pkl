module NotTranspiling

class NotTranspilingItems {
    `module`: String
    noEmit: Boolean
}

const NotTranspiling: NotTranspilingItems = new NotTranspilingItems {
    `module` = "preserve"
    noEmit = true
}
    
function addNotTranspilingItems(notTranspilingItems: NotTranspilingItems, useNotTranspiling: Boolean): Map = 
    if (useNotTranspiling) 
        notTranspilingItems.toMap()
    else
        Map()