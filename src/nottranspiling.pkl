module NotTranspiling

class NotTranspilingItems {
    `module`: String
    noEmit: Boolean
}

const NotTranspiling: NotTranspilingItems = new NotTranspilingItems {
    `module` = "preserve"
    noEmit = true
}
