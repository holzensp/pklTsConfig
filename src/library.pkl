module Library

class LibraryItems {
    declaration: Boolean
}

const Library: LibraryItems = new LibraryItems {
    declaration = true
}

function addLibraryItems(libraryItems: LibraryItems, useLibrary: Boolean): Map =
    if (useLibrary) 
        libraryItems.toMap()
    else
        Map()
